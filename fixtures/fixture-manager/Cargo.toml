[package]
name = "fixture-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
color-eyre = { version = "0.6.1", default-features = false }
clap = { version = "3.1.6", features = ["derive"] }
fixture-details = { path = "../fixture-details" }
